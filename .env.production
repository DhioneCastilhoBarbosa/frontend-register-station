# Produção — usada em `npm run build` / `vite build`
VITE_API_URL=https://api-register.api-castilho.com.br

# Credenciais e licença: defina no ambiente de CI ou em `.env.production.local` (não versionado)
# VITE_AUTH_EMAIL=
# VITE_AUTH_PASSWORD=
# VITE_LICENSE_CODE=
